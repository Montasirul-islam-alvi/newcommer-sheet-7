#include<bits\stdc++.h>
using namespace std;
void print(int x)
{
    if(x==0)
    {
        return;
    }
    print(x-1);
    cout<<"I love Recursion"<<endl;
}
int main()
{
    int n;
    cin>>n;
    print(n);
}
